{% extends 'base.html.twig' %}

{% block container %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    <div class="row justify-content-center">
        <div class="col-12 col-md-6 col-lg-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ include('/_recaptcha.html.twig',{name:'ticket_service'}) }}
            <button type="submit" class="btn btn-secondary">Записаться на сервис</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

